spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stack_over_flow
    username: root
    password: 2502
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true